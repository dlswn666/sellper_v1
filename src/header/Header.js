import React from 'react';
import MenuComponent from '../components/MenuComponent.js';

const Header = () => {
    return (
        <header>
            <MenuComponent />
        </header>
    );
};

export default Header;
