import React from 'react';
import MenuComponent from '../components/MenuComponent';

const Header = () => {
    return (
        <header>
            <MenuComponent />
        </header>
    );
};

export default Header;
